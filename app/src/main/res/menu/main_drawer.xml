<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
<group
    android:id="@+id/main_menu_group"
    android:checkableBehavior="single">
    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_search"
        android:title="@string/search_books" />
    <item
        android:id="@+id/action_my_results"
        android:icon="@drawable/ic_menu_book"
        android:title="@string/search_results" />
</group>
<item android:title="@string/logout">
    <menu>
        <group
            android:id="@+id/additional_menu_group"
            android:checkableBehavior="none">
            <item
                android:id="@+id/action_logout"
                android:checkable="false"
                android:icon="@drawable/ic_exit"
                android:title="@string/logout" />
        </group>
    </menu>
</item>
</menu>